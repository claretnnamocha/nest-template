{
  "$id": "custom-generator-schema",
  "type": "object",
  "properties": {
    "name": {
      "type": "string",
      "description": "The name of the service."
    },
    "path": {
      "type": "string",
      "description": "The path to create the service.",
      "default": "src"
    },
    "flat": {
      "type": "boolean",
      "description": "If true, does not create a folder for the service.",
      "default": false
    }
  },
  "required": ["name"]
}
